# v3.7 verification

Verified in the build workspace before packaging:

- Golden Master integrity: PASS
- Architecture verification: PASS
- Source reconstruction verification: PASS
- Unit tests: 87 / 87 PASS
- Data Truth: all 9 screens PASS
- Vercel route/workflow syntax checks: PASS
- Golden `main.js`, `main.css`, `404.html`, `favicon.svg`, `og.png`: unchanged
- No top-level source `api/` directory

Additional v3.7 tests verify:

- source-isolated guild/uploader train/holdout,
- generic opposite-state discovery,
- Light/Void state mechanics are not flattened to generic raid damage,
- wipe-enriched cast → enemy aura relation discovery,
- relation compilation into a scoreable generic failure-aura rule,
- Boss Learned component/recommendation output,
- enemy Buff/Debuff Deep WCL queries,
- zero-WCL RECOMPILE action,
- live `resumeAt` countdown in the corpus panel.

## Build limitation in this workspace

An actual `npm install` was attempted but the sandbox network operation timed out before dependencies were installed, so a complete local Vite/Nitro production build could not be executed here. The Vite/Nitro/Workflow dependency versions and route architecture are unchanged from the successfully deployed v3.6.3 integration; Vercel remains the production build integration check.
