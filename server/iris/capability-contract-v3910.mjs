import { getIrisCapabilityContract } from './capability-contract-v390.mjs';

export const IRIS_CAPABILITY_CONTRACT_V3910_VERSION='iris-capabilities-v3.9.10';

const extension=Object.freeze([
  Object.freeze({id:'knowledge.raid-catalog.current',status:'available',domain:'knowledge',autonomy:'automatic',endpoint:'GET /api/knowledge/raid-catalog',effect:'metadata-read+persist',description:'Discover the current official raid without any combat report. Blizzard classifies raids/bosses and WCL WorldData supplies operational IDs, partitions and difficulty metadata. No combat event query is allowed.'}),
  Object.freeze({id:'knowledge.raid.official-boss-difficulty-bootstrap',status:'available',domain:'knowledge',autonomy:'automatic',endpoint:'GET /api/knowledge/raid-catalog?official=1',effect:'official+structural-read+persist',description:'Bootstrap every current-raid boss and explicit WCL difficulty from Blizzard Journal plus build-pinned DB2 metadata. Difficulty name/player-range metadata resolves client DifficultyID; Journal encounter/section difficulty tables are applicability restrictions only.'}),
  Object.freeze({id:'knowledge.raid-learning.availability-preview',status:'available',domain:'knowledge',autonomy:'automatic',endpoint:'POST /api/knowledge/raid-learning-plan {action:preview}',effect:'read-only-plan',description:'Preview one metadata-only availability check per boss+difficulty. Executes zero network calls and zero combat-event calls.'}),
  Object.freeze({id:'knowledge.raid-learning.availability-refresh',status:'available',domain:'knowledge',autonomy:'bounded',endpoint:'POST /api/knowledge/raid-learning-plan {action:refresh,confirmExecution:true,previewFingerprint}',effect:'bounded-wcl-metadata-read',description:'Check whether public WCL ranking seed reports exist independently for each boss+difficulty. Ranking outcomes are discarded and no combat events are requested. Normal/Heroic availability never counts as Mythic evidence.'}),
  Object.freeze({id:'corpus.raid-reference.foundation-preview',status:'available',domain:'corpus',autonomy:'automatic',endpoint:'GET/POST /api/knowledge/raid-corpus-bootstrap {action:preview}',effect:'read-only-plan',description:'Build a zero-network plan for fresh-tier GLOBAL public reference corpora from persisted raid availability. Only public Normal/Heroic/Mythic scopes are eligible by default; LFR is excluded from the progression-reference default. Existing same-difficulty corpora are reused.'}),
  Object.freeze({id:'corpus.raid-reference.foundation-start',status:'available',domain:'corpus',autonomy:'bounded',endpoint:'POST /api/knowledge/raid-corpus-bootstrap {action:start,confirmExecution:true,previewFingerprint}',effect:'bounded-corpus-initialization',description:'Initialize a bounded foundation corpus for previewed public boss+difficulty scopes. Foundation is an early GLOBAL reference for Live/analysis, not accepted boss knowledge: it cannot satisfy Promotion and cannot borrow another difficulty. Initialization is bounded and worker acquisition remains checkpointed/rate-protected.'}),
  Object.freeze({id:'knowledge.mechanics.boss-difficulty-read',status:'available',domain:'knowledge',autonomy:'automatic',endpoint:'GET /api/wcl/mechanic-knowledge?journal=<id>&difficulty=<id>',effect:'read-only-persisted-view',description:'Render one boss+difficulty knowledge scope from persisted official, structural and same-difficulty empirical products. A report is optional; Normal, Heroic and Mythic evidence are never substituted. Executes zero network calls.'}),
  Object.freeze({id:'knowledge.ability.boss-difficulty',status:'available',domain:'knowledge',autonomy:'bounded',endpoint:'GET/POST /api/knowledge/ability?encounterId=<id>&difficulty=<id>',effect:'provider-aware-difficulty-scoped-knowledge',description:'Resolve encounter-scoped Ability Knowledge through exact official and structural difficulty views. Preview fingerprints include difficulty so Heroic/Mythic provider results cannot be replayed across scopes.'}),
  Object.freeze({id:'corpus.untouched-holdout.source-discovery-preview',status:'available',domain:'corpus',autonomy:'automatic',endpoint:'POST /api/wcl/untouched-holdout {action:discover-sources-preview}',effect:'read-only-plan',description:'Derive the frozen Stability candidate set and GLOBAL BOSS source lineage, then preview bounded unseen-source discovery at zero network.'}),
  Object.freeze({id:'corpus.untouched-holdout.source-discovery',status:'available',domain:'corpus',autonomy:'bounded',endpoint:'POST /api/wcl/untouched-holdout {action:discover-sources,confirmExecution:true,previewFingerprint}',effect:'metadata-network-read',description:'Discover independent unseen holdout sources from WCL metadata only. Ranking metrics are discarded, HOME/prior-learning lineage is excluded automatically, and combat event/table queries are forbidden.'}),
  Object.freeze({id:'corpus.untouched-holdout.reserve',status:'available',domain:'corpus',autonomy:'bounded',endpoint:'POST /api/wcl/untouched-holdout {action:reserve}',effect:'persist-holdout-precommit',description:'Freeze Stability fingerprint, candidates, unseen source set, seed reports and thresholds before holdout combat evidence. Reservation executes zero network calls.'}),
  Object.freeze({id:'corpus.untouched-holdout.acquire-combat-evidence-preview',status:'available',domain:'corpus',autonomy:'automatic',endpoint:'POST /api/wcl/untouched-holdout {action:acquire-evidence-preview,reservationFingerprint}',effect:'read-only-plan',description:'Preview bounded WCL evidence for an already frozen Holdout at zero network and without broadening candidates/sources/reports.'}),
  Object.freeze({id:'corpus.untouched-holdout.acquire-combat-evidence',status:'available',domain:'corpus',autonomy:'bounded',endpoint:'POST /api/wcl/untouched-holdout {action:acquire-evidence,reservationFingerprint,confirmExecution:true,previewFingerprint}',effect:'bounded-wcl-empirical-read',description:'Acquire only narrow paired same-fight evidence precommitted by a reservation-ready Holdout. Source expansion, outcome-based selection and automatic Promotion are forbidden.'}),
  Object.freeze({id:'corpus.untouched-holdout.evaluate',status:'available',domain:'corpus',autonomy:'bounded',endpoint:'POST /api/wcl/untouched-holdout {action:evaluate,reservationFingerprint}',effect:'persist-holdout-evaluation',description:'Evaluate only the compatible persisted automatic Holdout acquisition; never accepts caller-fabricated holdout evidence and never automatically promotes.'}),
]);

export function getIrisCapabilityContractV3910(){
  const base=getIrisCapabilityContract(),ids=new Set(base.capabilities.map(row=>row.id));
  return{
    ...base,version:IRIS_CAPABILITY_CONTRACT_V3910_VERSION,release:'3.9.10',
    purpose:'Machine-readable Iris contract with raid-first boss+difficulty knowledge, early GLOBAL public raid references and boss-agnostic Holdout.',
    documentation:[...base.documentation,'docs/IRIS-RAID-BOSS-DIFFICULTY-KNOWLEDGE-V1.md','docs/IRIS-GLOBAL-RAID-REFERENCE-V1.md','docs/IRIS-UNTOUCHED-HOLDOUT-V1.md'],
    invariants:{
      ...base.invariants,
      raidBossDifficultyScope:'Mechanics knowledge is raid → boss → difficulty. Official boss knowledge may exist with zero reports; GLOBAL WCL and AvoiD execution are optional overlays from the exact same difficulty only.',
      difficultyIsolation:'GLOBAL BOSS empirical products remain encounter+difficulty+partition scoped. Normal/Heroic may prioritize a later Mythic hypothesis but never count as Mythic observation, validation, Stability, Holdout or Promotion evidence.',
      difficultyIdentityMapping:'WCL and DB2 difficulty IDs are distinct namespaces. Resolve DB2 identity from Difficulty metadata and treat Journal difficulty tables only as applicability restrictions. Ambiguous mapping stays unresolved.',
      bossAgnosticGlobalLearning:'GLOBAL BOSS production learning is driven by encounter+difficulty+partition and persisted evidence/provider state; validation-boss constants may not enter generic runtime.',
      publicEvidenceAvailability:'Availability checks are metadata-only, difficulty-scoped and outcome-discarding. A public Heroic source says nothing about Mythic availability or evidence.',
      earlyGlobalReference:'A fresh-tier foundation corpus is a same-difficulty GLOBAL comparison baseline for Live/analysis, not accepted boss knowledge. It remains below Promotion gates, excludes HOME through normal corpus source policy, and must be enriched/verified independently before any accepted mechanic claim.',
      untouchedHoldout:'Holdout candidates, sources, seed-report metadata and thresholds are frozen before combat evidence; historical validation is never silently relabeled untouched.',
      holdoutSourceDiscovery:'Automatic Holdout source discovery may use bounded WCL metadata after a fingerprinted preview but may not inspect candidate combat outcomes before reservation.',
      holdoutCombatAcquisition:'Automatic Holdout combat acquisition requires a frozen reservation plus matching zero-network preview and cannot expand sources/candidates/reports or auto-promote.',
    },
    capabilities:[...base.capabilities,...extension.filter(row=>!ids.has(row.id))],
  };
}
export function findIrisCapabilityV3910(id){return getIrisCapabilityContractV3910().capabilities.find(row=>row.id===id)||null;}
