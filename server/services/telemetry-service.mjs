import { getTelemetry } from '../engines/telemetry-engine.mjs';import { runtimeConfig } from '../config/runtime.mjs';import { jsonResponse } from '../api/http.mjs';
export default async req=>{if(req.method!=='GET')return jsonResponse(405,{ok:false,error:'Method not allowed'});const u=new URL(req.url),reportCode=u.searchParams.get('report')||runtimeConfig.defaultReportCode,encounterId=u.searchParams.get('encounter'),difficulty=u.searchParams.get('difficulty'),debug=u.searchParams.get('debug')==='1';try{const data=await getTelemetry({reportCode,encounterId,difficulty,debug});if(!data)return jsonResponse(404,{ok:false,error:'Report not found',reportCode});return jsonResponse(200,{ok:true,...data},'public, max-age=30, s-maxage=30, stale-while-revalidate=60')}catch(e){return jsonResponse(500,{ok:false,error:e instanceof Error?e.message:String(e),reportCode})}};
