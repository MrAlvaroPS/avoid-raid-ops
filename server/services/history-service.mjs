import { getGuildHistory } from '../engines/history-engine.mjs';import { runtimeConfig } from '../config/runtime.mjs';import { jsonResponse } from '../api/http.mjs';
export default async req=>{if(req.method!=='GET')return jsonResponse(405,{ok:false,error:'Method not allowed'});const u=new URL(req.url),reportCode=u.searchParams.get('report')||runtimeConfig.defaultReportCode,guildId=Number(u.searchParams.get('guild')||runtimeConfig.guildId),encounterId=u.searchParams.get('encounter'),difficulty=u.searchParams.get('difficulty');try{const data=await getGuildHistory({reportCode,guildId,encounterId,difficulty});if(!data)return jsonResponse(404,{ok:false,error:'Current report not found',reportCode});return jsonResponse(200,{ok:true,...data},'public, max-age=300, s-maxage=300, stale-while-revalidate=600')}catch(e){return jsonResponse(500,{ok:false,error:e instanceof Error?e.message:String(e),reportCode,guildId})}};
