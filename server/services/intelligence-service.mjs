import { getEncounterIntelligence } from '../engines/intelligence-engine.mjs';import { runtimeConfig } from '../config/runtime.mjs';import { jsonResponse } from '../api/http.mjs';
const RELEASE='3.8.2';
export default async req=>{if(req.method!=='GET')return jsonResponse(405,{ok:false,error:'Method not allowed'});const u=new URL(req.url),reportCode=u.searchParams.get('report')||runtimeConfig.defaultReportCode,encounterId=u.searchParams.get('encounter');try{const data=await getEncounterIntelligence({reportCode,encounterId});if(!data)return jsonResponse(404,{ok:false,error:'Report not found',reportCode});return jsonResponse(200,{ok:true,...data,engineVersion:RELEASE},'public, max-age=30, s-maxage=30, stale-while-revalidate=60')}catch(e){return jsonResponse(500,{ok:false,error:e instanceof Error?e.message:String(e),reportCode})}};