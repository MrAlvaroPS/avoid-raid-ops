# v3.5 verification

## Passed locally

- Golden Master integrity: **PASS** (6 immutable files).
- Architecture verification: **PASS**.
- Reconstruction/string fidelity: **PASS**.
- Data Truth UI verification: **PASS** on all 9 screens.
- JavaScript / MJS syntax checks across `server`, `netlify`, source UI and deploy runtime: **PASS**.
- Node unit suite: **61/61 PASS**.
- New corpus-specific tests cover:
  - ranking JSON extraction/pagination hints;
  - truncated deep-stream exclusion;
  - thousands-scale publication gate;
  - candidate status below publication population;
  - generic Light/Void-style state-pair discovery;
  - WCL `wipeCalledTime=0` eligibility regression.

## Not executable in this sandbox

A real corpus build was not run because the WCL client credentials live only in the user's Netlify environment. The first deployed run is therefore also the integration test for the live `fightRankings` JSON shape and WCL query cost.

`npm run build:web` could not be executed because this sandbox does not have the project's npm dependencies installed and `npm install` timed out. This does not affect the current Netlify publish target (`deploy-preview/public` + Functions), which was syntax/test/fidelity checked; the complete React source is nevertheless included for the planned move away from compiled Golden-adapter deployment.

## First deployed checks

1. Open Mechanics and confirm the `Encounter Intelligence Corpus` panel appears.
2. Before starting, verify:
   `GET /api/wcl/corpus?encounter=3182` returns `{ "ok": true, "status": null }` on a fresh site store.
3. Start with 2,500 reports.
4. Confirm discovery increments ranking pages and `resolvedPartition` is populated.
5. Confirm wide reports and retained pull counts grow while WCL points remain above the protected reserve.
6. Confirm a rate-limit pause persists and resumes without resetting counts.
7. At completion inspect:
   `GET /api/wcl/corpus?encounter=3182&action=model`
8. Only when the model is `published` should `/api/wcl/intelligence` report rule-pack source `generated-wcl-corpus`; otherwise it must continue using the curated fallback.
