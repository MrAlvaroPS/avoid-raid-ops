{
  "name": "avoid-raid-ops-vercel",
  "private": true,
  "version": "0.3.9-4-vercel.0",
  "type": "module",
  "workspaces": ["apps/web", "packages/release"],
  "scripts": {
    "predev": "npm run prepare:css",
    "dev": "vite",
    "iris": "node --env-file=.env.local scripts/iris-local-worker.mjs",
    "dev:source": "npm --workspace @avoid/web run dev",
    "build:web": "npm --workspace @avoid/web run build",
    "prepare:css": "node scripts/build-active-css.mjs",
    "verify:css": "node scripts/verify-active-css-bundle.mjs",
    "verify:golden": "python3 scripts/verify_golden.py",
    "verify:architecture": "python3 scripts/verify_architecture.py",
    "verify:assets": "npm run prepare:css && npm run verify:css && node scripts/verify-active-assets.mjs",
    "verify:legacy-runtime": "node scripts/verify-legacy-runtime-ownership.mjs",
    "verify:reconstruction": "python3 scripts/verify_reconstruction.py",
    "verify:data-truth": "python3 scripts/verify_data_truth_active.py",
    "verify:release": "node scripts/verify-release.mjs",
    "verify:vercel": "node scripts/verify-vercel.mjs",
    "verify": "npm run verify:golden && npm run verify:architecture && npm run verify:assets && npm run verify:legacy-runtime && npm run verify:reconstruction && npm run verify:data-truth && npm run verify:release && npm run test:critical && npm test",
    "verify:all": "npm run verify && npm run build",
    "test": "node --test tests/unit/*.test.mjs",
    "test:critical": "node --test tests/critical/*.test.mjs",
    "build": "npm run verify:assets && npm run verify:legacy-runtime && npm run verify:vercel && npm run verify:release && npm run test:critical && npm test && vite build",
    "preversion": "npm run verify:assets && npm run verify:legacy-runtime && npm run verify:release && npm run verify:vercel && npm run test:critical",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vercel/blob": "2.6.1",
    "nitro": "3.0.260610-beta",
    "workflow": "5.0.0-beta.36"
  },
  "devDependencies": {"vite":"npm:rolldown-vite@7.2.5"},
  "overrides": {"vite":"npm:rolldown-vite@7.2.5"},
  "engines": {"node":"22.x"}
}
