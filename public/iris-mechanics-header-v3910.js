(()=>{
'use strict';
const RELEASE='3.9.12.0';
const mechanics=()=>String(document.querySelector('.page-head h1')?.textContent||'').trim().toLowerCase()==='mechanics';
const raidName=()=>String(document.querySelector('.iris-k-scope-raid b')?.textContent||'').trim();
const execution=()=>window.__AVOID_RAID_EXECUTION__||null;
const remember=(node,key)=>{if(node&&node.dataset[key]==null)node.dataset[key]=node.textContent||'';};
const setText=(node,text)=>{if(node&&String(node.textContent||'')!==String(text||''))node.textContent=String(text||'');};
const restoreText=(node,key)=>{if(node?.dataset[key]!=null){setText(node,node.dataset[key]);delete node.dataset[key];}};
function executionDetail(x){if(!x?.population?.pulls)return'AvoiD longitudinal boss+difficulty execution · awaiting observed pulls';const blocker=x.blocker?.name?` · blocker: ${x.blocker.name}`:' · no recurrent mechanic blocker';return`${x.population.pulls} pulls · ${x.population.reports||0} reports${blocker}`;}
function restore(){const top=document.querySelector('.topbar');if(top){const crumb=top.querySelector('.breadcrumbs'),raidSpan=crumb?.querySelectorAll('span')?.[1];restoreText(raidSpan,'irisOriginalText');for(const button of top.querySelectorAll('.selectors > button[data-iris-original-hidden]')){button.hidden=button.dataset.irisOriginalHidden==='1';delete button.dataset.irisOriginalHidden;}delete top.dataset.irisMechanicsHeaderRelease;}const banner=document.querySelector('.page-banner[data-iris-mechanics-banner-release]');if(banner){const badge=banner.querySelector('.badge'),title=banner.querySelector('h2'),copy=banner.querySelector('p'),stat=banner.querySelector('.banner-stat');restoreText(badge,'irisOriginalText');restoreText(title,'irisOriginalText');restoreText(copy,'irisOriginalText');restoreText(stat?.querySelector('label'),'irisOriginalText');restoreText(stat?.querySelector('b'),'irisOriginalText');restoreText(stat?.querySelector('small'),'irisOriginalText');delete banner.dataset.irisMechanicsBannerRelease;}}
function ownBanner(){const banner=document.querySelector('.canvas > .page-banner');if(!banner)return;const badge=banner.querySelector('.badge'),title=banner.querySelector('h2'),copy=banner.querySelector('p'),stat=banner.querySelector('.banner-stat'),label=stat?.querySelector('label'),value=stat?.querySelector('b'),small=stat?.querySelector('small'),x=execution();for(const node of [badge,title,copy,label,value,small])remember(node,'irisOriginalText');setText(badge,'AVOID EXECUTION INTELLIGENCE');setText(title,'Boss Mechanics');setText(copy,'Longitudinal AvoiD mechanic execution for this boss+difficulty, grounded by Iris boss knowledge and observed WCL pulls.');setText(label,'CURRENT MECHANICAL STATE');setText(value,x?.state?.label||'NO AVOID DATA');setText(small,executionDetail(x));if(stat)stat.dataset.executionTone=x?.state?.tone||'muted';banner.dataset.irisMechanicsBannerRelease=RELEASE;}
function own(){if(!mechanics()){restore();return;}const top=document.querySelector('.topbar');if(top){const crumb=top.querySelector('.breadcrumbs'),raidSpan=crumb?.querySelectorAll('span')?.[1],name=raidName();if(raidSpan){remember(raidSpan,'irisOriginalText');if(name)setText(raidSpan,name.toUpperCase());}for(const button of top.querySelectorAll('.selectors > button:not(.live)')){if(button.dataset.irisOriginalHidden==null)button.dataset.irisOriginalHidden=button.hidden?'1':'0';button.hidden=true;}top.dataset.irisMechanicsHeaderRelease=RELEASE;}ownBanner();}
let queued=false;function schedule(){if(queued)return;queued=true;queueMicrotask(()=>{queued=false;own();});}
new MutationObserver(schedule).observe(document.documentElement,{subtree:true,childList:true,characterData:true});
for(const event of ['popstate','hashchange','avoid:execution-context','avoid:active-report-data','avoid:raid-execution'])window.addEventListener(event,schedule);document.addEventListener('DOMContentLoaded',schedule,{once:true});window.addEventListener('load',schedule,{once:true});schedule();
})();
