# AvoiD Raid Ops v3.2 verification

## Golden / architecture

```text
GOLDEN VERIFICATION: PASS
- 6 immutable Golden files match manifest hashes
- source web Golden CSS/assets match byte-for-byte
- deploy-preview main.js/main.css/assets match byte-for-byte

ARCHITECTURE VERIFICATION: PASS
- 9 screen modules
- 9 navigation destinations
- 7 Golden mock datasets retained
- original UI classes covered
- thin Netlify Functions
- server/domain/storage/rule-pack layers present

RECONSTRUCTION VERIFICATION: PASS
- every original string literal from the 9 screens + AppShell survives the split
```

## Domain / adapter tests

18/18 Node tests pass.

Coverage includes:
- DamageTaken mechanics cannot become player names;
- numeric WCL ability IDs;
- role normalization;
- event-row actor counts;
- Belo'ren `1 → 2 → 1` semantic phases normalized to absolute Stage 3;
- raw / wipe-cutoff / first-death semantics;
- CombatantInfo gear/talent normalization;
- best-pull vs encounter player scopes;
- overlapping logger reports → one RaidSession;
- duplicate pull dedupe;
- two-stage history query strategy;
- no unfiltered Buff-event download;
- enemy-only mechanic casts;
- separate best-pull CombatantInfo query;
- `wipeCutoff` death query;
- null Reliability remains `— / PENDING`, never accidental `0`;
- Death Knight / Demon Hunter class-name canonicalization;
- optional DOM panel selectors are null-safe.

## Browser smoke test

A deterministic real-shaped Avoid/Belo'ren fixture was rendered in system Chromium against the actual Golden `main.js`, Golden `main.css`, v3.2 typography override and current WCL runtime.

Results:

```text
9/9 navigation screens clickable: PASS
Composition Roster Intelligence panel: 1
Roster rows: 20
Expandable equipment/talent detail: PASS
Death Knight class count: 2
Demon Hunter class count: 1
Reliability null values rendered: — / PENDING
Console errors: 0
Page errors: 0
```

Representative computed font sizes after the readability override:

```text
panel explanatory text: 10px
class-representation data: 10px
roster primary data: 10.5px
roster secondary data: 9px
roster table headers: 9px
```

The smoke test also discovered and fixed two adapter regressions before packaging:
- `Number(null)` was turning pending Reliability into `0`;
- one optional Damage & Healing legend could trigger a null `querySelectorAll` error.

## Syntax

All project `.js` / `.mjs` files are checked with `node --check` during packaging. See `syntax-results-v32.json` for the final count/result.

## Live-data boundary

The local browser verification uses deterministic data shaped from the real Avoid report, not a live WCL request. The deployable Netlify build must still be smoke-tested against the configured WCL credentials after upload. `debug=1` is provided specifically for flexible CombatantInfo/enemy-cast payload diagnostics.
