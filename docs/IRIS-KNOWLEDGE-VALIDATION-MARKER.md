# Iris knowledge boundary validation marker

This marker intentionally triggers a full branch validation after the GLOBAL BOSS vs HOME RAID knowledge-boundary implementation and audit. It has no runtime effect.
