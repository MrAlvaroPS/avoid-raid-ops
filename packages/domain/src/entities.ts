export type PullKey = { reportCode:string; fightId:number };
export type CharacterKey = { canonicalId?:number; region?:string; server?:string; normalizedName:string };
export type EngineStamp = { engineVersion:string; partition?:number; gameVersion?:number; rulePackVersion?:string };
