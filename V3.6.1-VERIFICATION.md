# AvoiD Raid Ops v3.6.2 — verification

Verification performed in the build workspace:

- `npm test`: **77/77 PASS**.
- Golden verification: **PASS**.
- Architecture verification: **PASS**.
- Source reconstruction verification: **PASS**.
- Data Truth verification: **PASS on all 9 screens**.
- Vercel adapter/workflow syntax checks: **PASS**.
- Invalid `workflow/sleep` import: **absent**.
- Composition regression coverage added for all 13 retail classes, class colours and Monk/Mystic Touch utility gap.

The full Vite/Nitro production build cannot be completed in this sandbox because dependency installation times out on network access. The specific Vercel build error supplied by the user is addressed according to the current Workflow SDK API, where `sleep` is exported from `workflow`. The next Vercel deployment is therefore the integration build check.
